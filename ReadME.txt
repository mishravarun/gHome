ReadME:

The folder Android_Arduino contains the android app that conencts to the arduino and also the app engine code, from where it receives a GCM notification.

The folder WearApp contains the android wear application where the actual classification is taking place and the the classified value is posted to the app engine servlet.

The arduino_code file contains the code for the arduino.
